<snippet>
  <content>
<![CDATA[
module ${1:Component} exposing (${2:..})

import Html exposing (Html, text)

-- TYPES

type Message
  = None

-- MODELS

type alias ${3:Model} =
  {
  }

-- RECORDS

initial${3:Model} : ${3:Model}
initial${3:Model} =
  {
  }

-- INIT

init : (${3:Model}, Cmd Message)
init =
  (initial${3:Model}, Cmd.none)

-- VIEW

view : ${3:Model} -> Html Message
view ${3/(.*)/\L\1\E/i} =
  text ""

-- UPDATE

update : Message -> ${3:Model} -> (${3:Model}, Cmd Message)
update message ${3/(.*)/\L\1\E/i} =
  (${3/(.*)/\L\1\E/i}, Cmd.none)
]]>
  </content>
  <tabTrigger>tthc</tabTrigger>
  <description>Elm Template (HTML Component)</description>
  <scope>source.elm</scope>
</snippet>
